rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Versão de depuração: Permite tudo para usuários logados
    // Recomendo restringir após o onboarding inicial
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
